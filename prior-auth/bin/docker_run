#!/usr/bin/env bash
BIN="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PWD=$(pwd)
source "$BIN"/SET

"$UTILS/docker_prepare" "$BIN"

echo "Running docker container $TAG for application $APPLICATION"
docker run --rm -d -p 9015:9015 --label "$APPLICATION"  "$TAG"