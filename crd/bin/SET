#!/usr/bin/env bash
BIN="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

APPLICATION='crd'

source "$BIN"/../../shared/SET
BUCKET_NAME=${PROJECT_ID}-${APPLICATION}
BUCKET="gs://$BUCKET_NAME"

if [[ -z ${CRD_IMAGE_TAG} ]]; then
  :
else
  IMAGE_TAG=$CRD_IMAGE_TAG
fi