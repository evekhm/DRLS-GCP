apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    application: prior-auth-new
  name: prior-auth-new
spec:
  replicas: 1
  selector:
    matchLabels:
      application: prior-auth-new
  template:
    metadata:
      labels:
        application: prior-auth-new
    spec:
      containers:
        - image: __IMAGE__
          name: prior-auth
          env:
          - name: TOKEN_BASE_URI
            valueFrom:
              configMapKeyRef:
                name: pa-config
                key: token_base_uri
          ports:
            - containerPort: 9000
          resources: {}
      imagePullSecrets:
      - name: regcred
      restartPolicy: Always