apiVersion: v1
kind: Service
metadata:
  labels:
    application: test-ehr
  name: test-ehr-service
spec:
  type: LoadBalancer
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    application: test-ehr
