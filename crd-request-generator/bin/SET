#!/usr/bin/env bash

BIN="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source "$BIN"/../../shared/SET

APPLICATION='crd-request-generator'
VERSION=v6jan
IMAGE="gcr.io/$PROJECT_ID/$APPLICATION:$VERSION"
